def preprocess_text(text):
    """Metni ön işleme (örneğin, küçük harfe çevirme, noktalama işaretlerini kaldırma)."""
    text = text.lower()
    # Diğer ön işleme adımları (gerekirse) buraya eklenebilir.
    return text